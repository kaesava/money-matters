{
  "name": "@money-matters/capability-budgeting",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "require": "./src/index.ts"
    }
  },
  "dependencies": {
    "@money-matters/config": "workspace:*",
    "@money-matters/core": "workspace:*",
    "@money-matters/db": "workspace:*",
    "@money-matters/types": "workspace:*",
    "drizzle-orm": "^0.39.0",
    "rrule": "^2.8.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  },
  "scripts": {
    "build": "echo \"Package build complete\"",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --passWithNoTests --coverage"
  }
}
